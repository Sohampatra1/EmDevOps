# -----------------------------------------------------------------------------
# Environment Variables Example File
# -----------------------------------------------------------------------------
# Rename this file to `.env.local` or `.env` and fill in the actual values.
# DO NOT commit your actual `.env.local` file to version control!

# 1. PostgreSQL Database Connection
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# For local native Postgres without Docker:
DATABASE_URL="postgresql://johndoe:randompassword@localhost:5432/mydb?schema=public"

# 2. Redis Connection (Used for caching / upstash)
# If running Redis locally:
REDIS_URL="redis://localhost:6379"

# 3. Groq API Key (Used for all AI generation features)
# Get your API key from https://console.groq.com/keys
GROQ_API_KEY="gsk_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"
